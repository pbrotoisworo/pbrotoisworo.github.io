language: python
python:
  - "3.8"
before_install:
  - python --version
  - pip install -U pip
  - pip install -U pytest
install:
  - pip install pandas
  - pip install folium
  - pip install Flask
  - pip install Frozen-Flask
  - pip install pytest-cov codecov
script:
  - pytest tests